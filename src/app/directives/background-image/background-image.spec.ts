import { BackgroundImage } from './background-image';

describe('BackgroundImage', () => {
  it('should create an instance', () => {
    const directive = new BackgroundImage();
    expect(directive).toBeTruthy();
  });
});
